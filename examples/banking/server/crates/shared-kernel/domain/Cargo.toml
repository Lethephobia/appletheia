[package]
name = "banking-shared-kernel-domain"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { workspace = true, features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
