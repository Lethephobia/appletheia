# Repository Guidance

Repository-specific conventions now live in skills:

- Use `appletheia-library-maintainer` for library, macro, and example maintenance.
- Use `appletheia-app-builder` for downstream CQRS / Event Sourcing application design.

